# App Icons

These icons are used for:

- Android “Add to Home screen” (via `public/site.webmanifest`)
- iOS home screen icon (via `apple-touch-icon` link in `index.html`)

Expected files:

- `public/icons/nphc-hc-192.png`
- `public/icons/nphc-hc-512.png`

